<!DOCTYPE html>
<html>
    <head>
		<!-- Make site responsive on mobile/tablet -->
		<meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Foodies' Zone</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
		<link rel="stylesheet" href="/stylesheets/main.css">
	  <style type="text/css">
		body {  padding-top: 70px; }
		.jumbotron {
		  color: #2c3e50;
		  background: #ecf0f1;

		}
		.navbar-inverse {
		  background: #2c3e50;
		  color: white;
		}
		.navbar-inverse .navbar-brand, .navbar-inverse a{
		  color:white;
		}
		.navbar-inverse .navbar-nav>li>a {
		  color: white;
		} 
		#sequence{
			z-index: 1;
		 }
		  
		 .navbar.scrolled {
			background: rgb(253 245 230);
			transition: background 500ms;
		}

	  </style>		
    </head>
    <body>
    <nav class="navbar navbar-default navbar-fixed-top" id="sequence">
		<div class="container">
			<div class="navbar-header">
				<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-nav-demo" aria-expanded="false" aria-controls="navbar">
				  <span class="sr-only">Toggle navigation</span>
				  <span class="icon-bar"></span>
				  <span class="icon-bar"></span>
				  <span class="icon-bar"></span>
				</button>

				<a class="navbar-brand" href="/"><span class="glyphicon glyphicon-cutlery"></span>  Foodies' Zone</a>
			</div>
			<div id="navbar" class="collapse navbar-collapse" id="bs-nav-demo">
			  	<ul class="nav navbar-nav">
					<li class="<%= typeof page !== 'undefined' && page === 'recipes' ? 'active' : '' %>"><a href="/recipes">Home</a></li>
				</ul>
				<ul class="nav navbar-nav navbar-right">
					<% if(!currentUser){ %>
					<li class="<%= typeof page !== 'undefined' && page === 'login' ? 'active' : '' %>"><a href="/login"><i class="fa fa-user"></i> Login</a></li>
						<li class="<%= typeof page !== 'undefined' && page === 'register' ? 'active' : '' %>"><a href="/register"><i class="fa fa-user-plus"></i> Sign Up</a></li>
					<% } else { %>
						<li><a href="#">Signed In As <%= currentUser.username %></a></li>
						<li><a href="/logout">Logout</a></li>
					<% } %>
				</ul>
				     <form class="navbar-form navbar-right form-inline" action="/recipes" method="GET">
        <div class="form-group">
				<input type="text" name="search" placeholder="Search Recipes" class="form-control"> 
				<input type="submit" value="Search" class="btn btn-default">
        </div>
      </form>
			</div>
		</div>
	</nav>	
	<!-- display flash message -->
	<div class="container">
		<% if (error && error.length > 0){ %>
			<div class="alert alert-danger" role="alert"> <!-- check out Boostrap->Component->alert for more options -->
				<%= error %>
			</div> 
		<% } %>
		<% if (success && success.length > 0){ %>
		<div class="alert alert-success" role="alert"> <!-- check out Boostrap->Component->alert for more options -->
			<%= success %>
		</div> 
		<% } %>
	</div>

	<div class="container">
	
		